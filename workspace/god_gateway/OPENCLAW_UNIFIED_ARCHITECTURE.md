# 🦞 OpenClaw Unified Architecture - God Gateway

## Overview

모든 시스템(LanceDB, FastAPI, Rust, Camel, AGen, Engram, PostgreSQL, Everything, Skills, Workflows, Agents, Vivace, ComfyUI, Antigravity, VSCode)을 통합하는 **완전한 AI 운영 체제**.

---

## 1. 시스템 구조

```
┌─────────────────────────────────────────────────────────────────────┐
│                         🦞 OpenClaw God Gateway                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │                      Presentation Layer (React)                    │ │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │ │
│  │  │   Dashboard │  │   Studio    │  │   Agent     │            │ │
│  │  │   (관리)    │  │   (창작)    │  │   (대화)    │            │ │
│  │  └─────────────┘  └─────────────┘  └─────────────┘            │ │
│  └─────────────────────────────┬───────────────────────────────────┘ │
│                                │ HTTP / WebSocket                       │
│  ┌─────────────────────────────┴───────────────────────────────────┐ │
│  │                    API Gateway (FastAPI)                           │ │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐           │ │
│  │  │ Auth Middle  │ │ Rate Limiter │ │ CORS Config  │           │ │
│  │  └──────────────┘ └──────────────┘ └──────────────┘           │ │
│  └─────────────────────────────┬───────────────────────────────────┘ │
│                                │                                        │
│  ┌─────────────────────────────┴───────────────────────────────────┐ │
│  │                     Core Services (병렬 처리)                       │ │
│  │                                                                     │ │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐      │ │
│  │  │  Agent    │ │ Creative  │ │ Memory    │ │  System   │      │ │
│  │  │  Engine   │ │  Engine   │ │  Engine   │ │  Engine   │      │ │
│  │  │ (Camel)  │ │(Vivace/UI)│ │(Engram)   │ │(System)   │      │ │
│  │  └─────┬─────┘ └─────┬─────┘ └─────┬─────┘ └─────┬─────┘      │ │
│  │        │             │             │             │              │ │
│  │        └─────────────┴─────┬───────┴─────────────┘              │ │
│  │                          │                                      │ │
│  └──────────────────────────┼──────────────────────────────────────┘ │
│                             ↓                                          │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │                    Data Layer (Multi-DB)                         │ │
│  │                                                                     │ │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐      │ │
│  │  │ LanceDB   │ │PostgreSQL │ │  SQLite   │ │ Everything│      │ │
│  │  │(Vector)   │ │   (SQL)   │ │(Task/Log) │ │(File)     │      │ │
│  │  └───────────┘ └───────────┘ └───────────┘ └───────────┘      │ │
│  │                                                                     │ │
│  └─────────────────────────────────────────────────────────────────┘ │
│                                                                       │
├─────────────────────────────────────────────────────────────────────┤
│                    External Integrations                               │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐          │ │
│  │  VSCode   │ │  ComfyUI  │ │  Rust     │ │ Antigrav- │          │ │
│  │  (Editor) │ │  (Image)  │ │ (Core)    │ │   ity     │          │ │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘          │ │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 2. Core Components

### 2.1 Agent Engine (Camel-AI 기반)

```python
# agents/camel_agent.py
from camel.agents import ChatAgent
from camel.models import OpenAIModel
from camel.messages import BaseMessage

class OpenClawAgent:
    def __init__(self):
        self.agent = ChatAgent(
            model=OpenAIModel(api_key=...),
            system_prompt=OPENCLAW_PERSONA
        )
    
    async def execute(self, task: str, context: dict) -> str:
        # 의도 분석 → 작업 분배 → 결과 통합
        return await self.agent.run(task)
```

### 2.2 Creative Engine (Vivace + ComfyUI)

```python
# engines/creative_engine.py
class CreativeEngine:
    async def generate_music(self, prompt: str, engine: str = "ace15"):
        if engine == "ace15":
            return await self.call_ace15_api(prompt)
        elif engine == "vivace":
            return await self.call_vivace_api(prompt)
    
    async def generate_image(self, prompt: str, engine: str = "comfyui"):
        return await self.call_comfyui_api(prompt)
```

### 2.3 Memory Engine (Engram + LanceDB)

```python
# engines/memory_engine.py
class MemoryEngine:
    async def remember(self, content: str, embedding: list):
        # LanceDB에 벡터 저장
        await self.lance_db.table("memories").add({
            "content": content,
            "embedding": embedding,
            "timestamp": datetime.now()
        })
    
    async def recall(self, query: str) -> list:
        # 유사도 검색
        results = await self.lance_db.query(query)
        return results
```

---

## 3. API Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/api/agent/execute` | 에이전트 명령 실행 |
| POST | `/api/creative/music` | 음악 생성 |
| POST | `/api/creative/image` | 이미지 생성 |
| POST | `/api/creative/video` | 비디오 생성 |
| GET | `/api/memory/search` | 기억 검색 |
| POST | `/api/memory/save` | 기억 저장 |
| GET | `/api/files/search` | Everything 파일 검색 |
| GET | `/api/system/status` | 시스템 상태 |

---

## 4. Todo List

- [ ] FastAPI Gateway 서버 구현
- [ ] Camel Agent Integration
- [ ] LanceDB Vector Search 구현
- [ ] Engram Memory Bridge
- [ ] Everything Search 연동
- [ ] Skills/Workflows/Agents 통합
- [ ] Vivace/ComfyUI 통합
- [ ] Rust Core 연결 (FFI)
- [ ] React Dashboard 구현
- [ ] VSCode Extension 구현
- [ ] 전체 테스트 및 배포

---

## 5. 실행 명령

```bash
# 개발 모드
python -m uvicorn main:app --reload --port 8095

# 프로덕션
gunicorn -k uvicorn.workers.UvicornWorker main:app --bind 0.0.0.0:8095
```

---

**Version**: 1.0.0  
**Status**: Planning
